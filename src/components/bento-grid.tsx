// import { Card, CardContent } from '@/components/Card';

interface Step {
  title: string;
  description: string;
  icon?: React.ReactNode;
  accent?: string;
}

const BentoGrid = () => {
  return <div></div>;
};
